{
  "name": "web-rtc-app",
  "version": "1.0.0",
  "description": "web RTC app with NodeJS and SocketIO",
  "main": "index.js",
  "author": "kanishev",
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "set NODE_ENV=dev && server.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "peer": "^0.6.1",
    "socket.io": "^4.3.2"
  }
}
